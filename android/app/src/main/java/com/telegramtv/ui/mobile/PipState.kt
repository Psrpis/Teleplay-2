package com.telegramtv.ui.mobile

import androidx.compose.runtime.compositionLocalOf

/** Shared player state exposed by the single native MainActivity. */
val LocalPipMode = compositionLocalOf { false }
